version: "3.8"

services:
  nuxt:
    build: .
    container_name: nuxt
    restart: unless-stopped
    env_file: .env
    environment:
      NUXT_PUBLIC_STRAPI_URL: ${NUXT_PUBLIC_STRAPI_URL}
      NUXT_PUBLIC_STRAPI_TOKEN: ${NUXT_PUBLIC_STRAPI_TOKEN}
      NUXT_PUBLIC_STRAPI_PUBLIC_URL: ${NUXT_PUBLIC_STRAPI_PUBLIC_URL}
      NUXT_PUBLIC_MAILERLITE_API_KEY: ${NUXT_PUBLIC_MAILERLITE_API_KEY}
      NODE_ENV: production
    ports:
      - "3000:3000"
    networks:
      - giro_network

networks:
  giro_network:
    external: true